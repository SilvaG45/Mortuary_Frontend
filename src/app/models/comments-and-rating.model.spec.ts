import { CommentsAndRating } from './comments-and-rating.model';

describe('CommentsAndRating', () => {
  it('should create an instance', () => {
    expect(new CommentsAndRating()).toBeTruthy();
  });
});
